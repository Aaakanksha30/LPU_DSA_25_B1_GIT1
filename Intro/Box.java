import java.util.*;
/*public class Box {
    double length;
    double width;
    double height;

    public Box(double length, double width, double height) {
        this.length = length;
        this.width = width;
        this.height = height;
    }

    public double calculateVolume() {
        return length * width * height;
    }

    public double calculateSurfaceArea() {
        return 2 * ((length * width) + (length * height) + (width * height));
    }

    public static void main(String[] args) {
        
        Box box1 = new Box(10.0, 5.0, 2.0);
        System.out.println("Box 1 Volume: " + box1.calculateVolume());
        System.out.println("Box 1 Surface Area: " + box1.calculateSurfaceArea());

        System.out.println("\n---");
        Box box2 = new Box(7.5, 3.0, 4.0);
        System.out.println("Box 2 Volume: " + box2.calculateVolume());
        System.out.println("Box 2 Surface Area: " + box2.calculateSurfaceArea());
    }
}*/
// A constructor is a sepecial member function which is called automatically when an object is created.
//It will have same name as that of class.
//It does not have a return type.


